# Prolog Hapiness Places

## Generate Database

### Requirements

Install pandas
``` sh
pip3 install pandas --user
```

### Generating

From project root, run:

``` sh
./parser/generate_all_databases.py
```

All needed databases will be stored on `knowledge_databases`
